# ПРОГРАММУЛЕЧКА
Для того, чтобы интерфейс работал, нужна библиотека PyQt5 (на ней работает интерфейс)

Установить можно так:
`pip install pyqt5`

Еще нужна библиотека paramiko (на ней работает SSH соединение)

Устанавливаем
`pip install paramiko`

Запускаем, жмём на иконфу 'OFF' - тем самым подключаемся по SSH. Потом жмём на вторую кнопку, чтобы запустить скрипт. Работаем.

Есть третья кнопка:
  После подключения по SSH можно скидывать выбранные файлы в директорию проекта. Файлы кидаются всегда в одно и то же место, но выбирать ЧТО кидать можно самостоятельно.

## Управление

Всё просто.
```
w -     Движение вперёд
s -     Движение назад
a -     Поворот сервопривода влево
d -     Поворот сервопривода вправо
shift - Повысить скорость для движения вперёд
ctrl -  Понизить скорость для движения вперёд
r -     Затормозить, работает пока так себе, но для крайнего случая сойдет
k -     Перезагрузить Arduino
p -     Запустить автоскрипт (так себе название, но лучше не придумал) на выполнение
c -     Безопасно выключить программу на распберри, после выключения программу можно перезапустить
```

## Проблемы
1. Менять скорость при движении назад пока нельзя
2. Автоперезагрузка ардуино перестала работать
3. То, что перезагрузка ардуино нужна в принципе - это проблема
4. При "грязном" выходе падает сокет и программу невозможно запустить без перезагрузки распберри. Иногда ловится мистический баг, и сокет падает даже при "безопасном" выходе
